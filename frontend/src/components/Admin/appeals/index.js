/**
 * Admin Appeals Components
 *
 * Components for appeal management in the admin panel.
 */

// Re-export from parent for now - can be moved here in future refactors
export { default as AppealsList } from '../AppealsList';
export { default as AppealReviewModal } from '../AppealReviewModal';
