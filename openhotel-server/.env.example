# OpenHotel Server Environment Variables
# Copy to .env for local development
# Render.com sets these via render.yaml

# ===========================================
# Server Configuration
# ===========================================
# Port for the server (Render.com provides this)
PORT=3005

# Frontend URL for CORS (set automatically by Render)
FRONTEND_URL=http://localhost:5173
# Production example:
# FRONTEND_URL=https://gacha-frontend.onrender.com

# ===========================================
# Authentication
# ===========================================
# Set to "true" to require authentication for connections
AUTH_ENABLED=false

# ===========================================
# Database
# ===========================================
# OpenHotel uses Deno KV by default
# Data persists in /app/data on Render (see render.yaml disk config)
