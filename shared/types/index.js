/**
 * Shared Type Definitions
 *
 * Central export for all shared type definitions.
 * These types are used by both backend and frontend.
 */

const essenceTap = require('./essenceTap');

module.exports = {
  ...essenceTap
};
